confirmModulesPurge=false
# Without this, pnpm silently runs `pnpm install --production` (wiping
# devDependencies like electron-vite/@nestjs/cli) whenever it thinks
# node_modules is out of sync with the lockfile — which the bundle:server
# script's own `pnpm deploy --prod` step reliably triggers for every pnpm
# command that runs afterward. Cost this project its devDependencies twice.
# These two settings don't actually prevent that wipe (only the interactive
# confirmation) — package:desktop's own `pnpm install --frozen-lockfile`
# right after `pnpm bundle:server` is what undoes it, immediately before
# anything that needs those devDependencies (electron-vite, electron-builder,
# nest) runs. Cost this project its devDependencies three times now.
verify-deps-before-run=false
